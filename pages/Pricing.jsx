import Nav from "../components/nav-page";
import style from "./Pricing.module.css"


function Price() {
  return (
    <div className={`${style.price} ${style.fade}`}>
      <Nav />

      <section>

      </section>
    </div>
  );
}

export default Price;
