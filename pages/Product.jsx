import Nav from "../components/nav-page";
import style from "./Product.module.css"

function Product() {
  return (
    <div className={`${style.product} ${style.fade}`}>
      <Nav />

      <section>

      </section>
    </div>
  );
}

export default Product;
